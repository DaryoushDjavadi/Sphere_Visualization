# Sphere_Visualization

Zusätzlich im Ordner **[hofdorf/](hofdorf/)**: Multiplayer-Farmspiel (eigener Hof + geteilte Stadt).

```bash
cd hofdorf && npm install && npm start
# → http://localhost:3847
```
